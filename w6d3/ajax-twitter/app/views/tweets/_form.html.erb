<h2>New Tweet!</h2>
<form class="tweet-compose" action="/tweets" method="POST">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea name="tweet[content]"></textarea>
    <strong class= ".chars-left">Characters left: 140</strong>
  </label>
  <br>

  <label>
    Mention
    <select name="tweet[mentioned_user_ids][]" multiple>
      <% User.all.each do |user| %>
        <option value="<%= user.id %>"><%= user.username %></option>
      <% end %>
    </select>
    <strong>Hold (ctrl/cmd) to select multiple mentions.</strong>
  </label>
  <br>

  <input type="Submit" value="Post Tweet!">
</form>
